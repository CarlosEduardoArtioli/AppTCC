// Model dos dados do dispositivo

export class Room {
    constructor(
        public $key: string,
        public name: string,
        public icon: string,
        public iconName: string,
    ) { }
}

// Models são os "Modelos" de dados de um objeto
